// Copy this file to secrets.h (same folder) and fill in your values.
// secrets.h is gitignored so your credentials never reach the repository.
#pragma once

const char *WIFI_SSID = "your-2.4GHz-network";
const char *WIFI_PASS = "your-wifi-password";

// Must match DEVICE_API_KEY in the backend's .env.
const char *DEVICE_KEY = "dev-device-key-change-me";
